import colors from './colors'
const { white, dark } = colors;

export default {
  palette: {
    type: "light",
    primary: {
      main: white,
    },
    secondary: {
      main: dark,
    },
  }
}
